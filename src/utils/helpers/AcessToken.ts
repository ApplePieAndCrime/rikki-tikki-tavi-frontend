// to get the current token (it will be used in fetch)
export async function getToken() {}

// to set token after login, logout or registration
export async function setToken() {}

// to check is the user logged in
export async function isLoggedIn() {}

// to give the provider a function that should be called after any token change
export async function subscribe() {}

// to remove subscriber
export async function unsubscribe() {}
